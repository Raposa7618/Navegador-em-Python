# Navegador-em-Python
A browser written in Python for a local company to replace the heavy Google Chrome, being simple, practical, and without saving data.

Required packages:
pip install PyQt5 PyQtWebEngine
